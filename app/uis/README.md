Vite + React frontend (uis)

Setup:

1. In the `app/uis` folder run `npm install`
2. Start dev server: `npm run dev` (defaults to port 5173)

The frontend expects the backend at `/api/*` (same host). Use a proxy or run both locally.
